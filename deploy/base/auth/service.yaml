apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: mozi
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: mozi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: mozi
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
