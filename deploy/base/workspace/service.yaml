apiVersion: v1
kind: Service
metadata:
  name: workspace-service
  namespace: mozi
  labels:
    app.kubernetes.io/name: workspace-service
    app.kubernetes.io/part-of: mozi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: workspace-service
    app.kubernetes.io/part-of: mozi
  ports:
    - name: http
      port: 3002
      targetPort: http
      protocol: TCP
