apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: mozi
  labels:
    app.kubernetes.io/name: workflow-service
    app.kubernetes.io/part-of: mozi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: workflow-service
    app.kubernetes.io/part-of: mozi
  ports:
    - name: http
      port: 3004
      targetPort: http
      protocol: TCP
