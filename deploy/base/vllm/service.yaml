apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: mozi
  labels:
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: mozi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: mozi
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
