apiVersion: v1
kind: Service
metadata:
  name: mcp-service
  namespace: mozi
  labels:
    app.kubernetes.io/name: mcp-service
    app.kubernetes.io/part-of: mozi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-service
    app.kubernetes.io/part-of: mozi
  ports:
    - name: http
      port: 3005
      targetPort: http
      protocol: TCP
