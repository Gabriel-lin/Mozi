# GitHub OAuth App — Device Flow
# 在 https://github.com/settings/applications/new 创建 OAuth App
# 勾选 "Enable Device Flow"
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# assistant-ui Cloud（与文档站 NEXT_PUBLIC_ASSISTANT_BASE_URL 同义）
# 必须是你在 https://cloud.assistant-ui.com 项目里复制的「Frontend API」地址，例如：
#   VITE_PUBLIC_ASSISTANT_BASE_URL=https://proj-xxxxxxxx.assistant-api.com
# 不要填 DeepSeek / OpenAI / Groq 等模型 API（如 https://api.deepseek.com），否则浏览器会请求
#   …/v1/auth/tokens/anonymous 并得到 401。
# 文档：https://www.assistant-ui.com/docs/cloud/ai-sdk-assistant-ui
# VITE_PUBLIC_ASSISTANT_BASE_URL=

# --- 浏览器直连大模型（OpenAI 兼容 API，如 DeepSeek）---
# 设置 VITE_LLM_API_KEY 后不再使用 Assistant Cloud；请求由浏览器直接发往模型服务商。
# 安全提示：密钥会出现在前端构建产物中，仅适合本地/可信环境；线上请用服务端转发。
# VITE_LLM_API_KEY=
# VITE_LLM_BASE_URL=https://api.deepseek.com/v1
# VITE_LLM_MODEL=deepseek-chat
# 若未设置 VITE_LLM_BASE_URL，会回退使用 VITE_PUBLIC_ASSISTANT_BASE_URL（便于沿用同一变量填模型地址）。
#
# 直连大模型须走 Chat Completions（/v1/chat/completions）。AI SDK 里请用 openai.chat(model)，不要用默认的 /v1/responses。
#
# 可选：pnpm dev 时若浏览器直连模型域名遇 CORS，可启用 dev 代理（仅本地，不写入 VITE_）：
# LLM_DEV_PROXY_TARGET=https://api.deepseek.com
# VITE_LLM_BASE_URL=http://localhost:3000/llm-proxy/v1
